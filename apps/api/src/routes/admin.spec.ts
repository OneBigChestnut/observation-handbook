import {fileURLToPath}from"node:url";import{migrate}from"drizzle-orm/better-sqlite3/migrator";import{describe,expect,it}from"vitest";import{buildApp}from"../app.js";import{getApiConfig}from"../config.js";import{openDatabase}from"../db/client.js";import{accounts}from"../db/schema.js";import{hashPassword}from"../password.js";
describe("admin",()=>it("only permits a super admin to view audit data",async()=>{const db=openDatabase(":memory:");migrate(db,{migrationsFolder:fileURLToPath(new URL("../../drizzle",import.meta.url))});await db.insert(accounts).values([{id:"s",username:"s",passwordHash:await hashPassword("correct-horse-battery-staple"),platformRole:"super_admin",createdAt:new Date()},{id:"u",username:"u",passwordHash:await hashPassword("correct-horse-battery-staple"),createdAt:new Date()}]);const app=await buildApp(db,getApiConfig({SESSION_SECRET:"a".repeat(32)}));const login=async(username:string)=>(await app.inject({method:"POST",url:"/api/auth/login",payload:{username,password:"correct-horse-battery-staple"}})).headers["set-cookie"] as string;expect((await app.inject({method:"GET",url:"/api/admin/audit",headers:{cookie:await login("u")}})).statusCode).toBe(403);expect((await app.inject({method:"GET",url:"/api/admin/audit",headers:{cookie:await login("s")}})).statusCode).toBe(200);await app.close()}));
