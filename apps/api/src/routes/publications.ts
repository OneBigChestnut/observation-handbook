import { randomUUID } from "node:crypto"; import { and, desc, eq } from "drizzle-orm"; import type { FastifyPluginAsync } from "fastify"; import { requireFamilyAdmin } from "@observation-handbook/domain"; import { getActorFromToken } from "../auth.js"; import type { ApiConfig } from "../config.js"; import type { AppDatabase } from "../db/client.js"; import { children, handbookPublications, handbooks } from "../db/schema.js";
type Opt={database:AppDatabase;config:ApiConfig}; export const registerPublicationRoutes:FastifyPluginAsync<Opt>=async(app,o)=>{const actor=async(t?:string)=>t?getActorFromToken(o.database,t):null;
app.get("/api/publications",async(r,reply)=>{if(!await actor(r.cookies[o.config.sessionCookie.name]))return reply.code(401).send({code:"AUTH_REQUIRED"});const rows=await o.database.select().from(handbookPublications).where(eq(handbookPublications.state,"published")).orderBy(desc(handbookPublications.publishedAt));return{publications:rows.map(row=>({id:row.id,...JSON.parse(row.snapshot),publishedAt:row.publishedAt}))}});
app.get<{Params:{publicationId:string}}>("/api/publications/:publicationId",async(r,reply)=>{if(!await actor(r.cookies[o.config.sessionCookie.name]))return reply.code(401).send({code:"AUTH_REQUIRED"});const row=await o.database.query.handbookPublications.findFirst({where:and(eq(handbookPublications.id,r.params.publicationId),eq(handbookPublications.state,"published"))});if(!row)return reply.code(404).send({code:"PUBLICATION_NOT_FOUND"});return{publication:{id:row.id,...JSON.parse(row.snapshot),publishedAt:row.publishedAt}}});
app.post<{Params:{handbookId:string}}>("/api/handbooks/:handbookId/publish",async(r,reply)=>{const a=await actor(r.cookies[o.config.sessionCookie.name]);if(!a)return reply.code(401).send({code:"AUTH_REQUIRED"});const h=await o.database.query.handbooks.findFirst({where:eq(handbooks.id,r.params.handbookId)});if(!h)return reply.code(404).send({code:"HANDBOOK_NOT_FOUND"});const c=await o.database.query.children.findFirst({where:eq(children.id,h.childId)});if(!c)return reply.code(404).send();try{requireFamilyAdmin(a,c.familyId)}catch{return reply.code(403).send({code:"FAMILY_ADMIN_REQUIRED"})}const now=new Date(),row={id:randomUUID(),handbookId:h.id,childId:c.id,snapshot:JSON.stringify({title:h.title,introduction:h.introduction,childName:c.name,cardCount:0}),state:"published" as const,publishedAt:now,withdrawnAt:null};await o.database.insert(handbookPublications).values(row);return reply.code(201).send({publication:{id:row.id,...JSON.parse(row.snapshot),publishedAt:now}})});
app.post<{Params:{publicationId:string}}>("/api/publications/:publicationId/withdraw",async(r,reply)=>{const a=await actor(r.cookies[o.config.sessionCookie.name]);if(!a)return reply.code(401).send({code:"AUTH_REQUIRED"});const row=await o.database.query.handbookPublications.findFirst({where:eq(handbookPublications.id,r.params.publicationId)});if(!row)return reply.code(404).send({code:"PUBLICATION_NOT_FOUND"});const c=await o.database.query.children.findFirst({where:eq(children.id,row.childId)});if(!c)return reply.code(404).send();try{requireFamilyAdmin(a,c.familyId)}catch{return reply.code(403).send({code:"FAMILY_ADMIN_REQUIRED"})}await o.database.update(handbookPublications).set({state:"withdrawn",withdrawnAt:new Date()}).where(eq(handbookPublications.id,row.id));return reply.code(204).send();});};
