import { fileURLToPath } from "node:url"; import { migrate } from "drizzle-orm/better-sqlite3/migrator"; import { describe,expect,it } from "vitest"; import { buildApp } from "../app.js"; import { getApiConfig } from "../config.js"; import { openDatabase } from "../db/client.js"; import { accounts,children,families,familyMemberships,handbooks } from "../db/schema.js"; import { hashPassword } from "../password.js";
describe("exports",()=>it("creates immutable A5 screen and print jobs and denies readers",async()=>{const db=openDatabase(":memory:");migrate(db,{migrationsFolder:fileURLToPath(new URL("../../drizzle",import.meta.url))});await db.insert(accounts).values([{id:"a",username:"a",passwordHash:await hashPassword("correct-horse-battery-staple"),createdAt:new Date()},{id:"r",username:"r",passwordHash:await hashPassword("correct-horse-battery-staple"),createdAt:new Date()}]);await db.insert(families).values({id:"f",name:"f",createdAt:new Date()});await db.insert(familyMemberships).values([{accountId:"a",familyId:"f",role:"admin"},{accountId:"r",familyId:"f",role:"reader"}]);await db.insert(children).values({id:"c",familyId:"f",name:"c",createdAt:new Date()});await db.insert(handbooks).values({id:"h",childId:"c",title:"h",introduction:"i",startedAt:"2026-01-01",visibility:"family",createdAt:new Date(),updatedAt:new Date()});const app=await buildApp(db,getApiConfig({SESSION_SECRET:"a".repeat(32)}));const login=async(username:string)=>(await app.inject({method:"POST",url:"/api/auth/login",payload:{username,password:"correct-horse-battery-staple"}})).headers["set-cookie"] as string;const a=await login("a"),r=await login("r");expect((await app.inject({method:"POST",url:"/api/children/c/exports",headers:{cookie:r},payload:{handbookId:"h",kind:"screen"}})).statusCode).toBe(403);const print=await app.inject({method:"POST",url:"/api/children/c/exports",headers:{cookie:a},payload:{handbookId:"h",kind:"print"}});expect(JSON.parse(print.json().export.snapshot).format).toMatchObject({paperSize:"A5",bleedMm:3,cropMarks:true});await app.close();}));
