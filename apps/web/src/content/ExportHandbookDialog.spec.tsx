// @vitest-environment jsdom
import "@testing-library/jest-dom/vitest"; import { fireEvent, render, screen } from "@testing-library/react"; import { expect,it,vi } from "vitest"; import { ExportHandbookDialog } from "./ExportHandbookDialog.js";
it("creates a print export from the header dialog flow",async()=>{const createExport=vi.fn().mockResolvedValue({});render(<ExportHandbookDialog childId="c" handbooks={[{id:"h",title:"手册",introduction:"",startedAt:"",completedAt:null,status:"ongoing",cardCount:1,tagCount:0,cardIds:[],tagIds:[]}]} createExport={createExport} onCreated={vi.fn()}/>);fireEvent.click(screen.getByLabelText("印刷 PDF（3mm 出血）"));fireEvent.click(screen.getByRole("button",{name:"确认生成"}));expect(createExport).toHaveBeenCalledWith("c",{handbookId:"h",kind:"print"});});
